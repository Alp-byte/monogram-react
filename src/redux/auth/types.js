const AuthTypes = {};

AuthTypes.getProfile = 'GET_PROFILE';

AuthTypes.login = ['LOGIN'];

AuthTypes.signup = ['SIGNUP'];
AuthTypes.logout = ['LOGOUT'];
AuthTypes.forgot = ['FORGOT'];
AuthTypes.resetPassword = ['RESET_PASSWORD'];

export default AuthTypes;
